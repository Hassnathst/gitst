# Use a pipeline as a high-level helper
from transformers import pipeline

pipe = pipeline("text-generation", model="IAAR-Shanghai/xVerify-0.5B-I")
messages = [
    {"role": "user", "content": "Who are you?"},
]
pipe(messages)
